<!-- NAVBAR -->
<header class="site-navbar mt-3">
  <div class="container-fluid">
    <div class="row align-items-center">
      
      <!-- Logo -->
      <div class="site-logo col-6">
        <a href="{{ path('app_home') }}">
          <img src="{{ asset('images/logo.png') }}" alt="Logo" class="img-fluid" style="max-height: 50px;">
        </a>
      </div>

      <!-- Navigation principale -->
      <nav class="mx-auto site-navigation">
        <ul class="site-menu js-clone-nav d-none d-xl-block d-lg-none ml-0 pl-0">
          <li>
            <a href="{{ path('app_home') }}" class="nav-link {% if app.request.get('_route') == 'app_home' %}active{% endif %}">Accueil</a>
          </li>
          <li>
            <a href="{{ path('app_about') }}" class="nav-link {% if app.request.get('_route') == 'app_about' %}active{% endif %}">À propos de nous</a>
          </li>
          {% if statut %}
          <li>
            <a href="{{ path('abo-voir.offre') }}" class="nav-link {% if app.request.get('_route') == 'abo-voir.offre' %}active{% endif %}">Toutes les offres</a>
          </li>
          {% endif %}
          <li>
            <a href="{{ path('app_contact') }}" class="nav-link {% if app.request.get('_route') == 'app_contact' %}active{% endif %}">Contact</a>
          </li>
          {% if not statut %}
          <li>
            <a href="{{ path('connexion.abonne') }}" class="nav-link {% if app.request.get('_route') == 'connexion.abonne' %}active{% endif %}">S'identifier</a>
          </li>
          {% endif %}
        </ul>
      </nav>

      <!-- Menu de droite -->
      <div class="right-cta-menu text-right d-flex align-items-center col-6">
        {% if not statut %}          
        <div class="ml-auto">
          <a href="{{ path('connexion.abonne') }}" class="btn btn-primary border-width-2 d-none d-lg-inline-block">
            <span class="mr-2 icon-lock_outline"></span>S'identifier
          </a>
        </div>
        {% endif %}
        <!-- Bouton mobile -->
        <button class="site-menu-toggle js-menu-toggle d-inline-block d-xl-none mt-lg-2 ml-3" aria-label="Ouvrir le menu">
          <span class="icon-menu h3 m-0 p-0 mt-2"></span>
        </button>
      </div>

    </div>
  </div>
</header>
