    <footer class="site-footer">

      <a href="#top" class="smoothscroll scroll-top">
        <span class="icon-keyboard_arrow_up"></span>
      </a>
        <div class="text-center">
          <div class="col-12">
            <h3>Nous suivre</h3>
                <a href="{{monSite.lienFacebook}}"><span class="icon-facebook"></span></a>
                <a href="{{monSite.lienTwitter}}"><span class="icon-twitter"></span></a>
                <a href="{{monSite.lienInstagram}}"><span class="icon-instagram"></span></a>
                <a href="{{monSite.linkedIn}}"><span class="icon-linkedin"></span></a>

            {% if statut %}
              <h6>
                  <a href="{{ path('app_logout') }}">Se déconnecter</a>
                  <span> Ou </span>
                  <form action="{{ path('Abonne_unsubscribe', { 'id': idAbonne }) }}" method="POST" onsubmit="return confirm('Voulez-vous vraiment vous désabonner ?')">
                      <input type="hidden" name="_token" value="{{ csrf_token('unsubscribe') }}">
                      <button type="submit" >Se désabonner</button>
                  </form>
              </h6>
            {% endif %}
              <p class="copyright"><small>
              Copyright &copy;<script>document.write(new Date().getFullYear());</script> All rights reserved {{monSite.nomPlateforme}}| Powered by <a href="https://elembo-tech.com" target="_blank" >ElemboTech</a></small></p>
          </div>
        </div>
    </footer>