{% extends 'basesecond.html.twig' %}

{% block title %} Formulaire de Ville {% endblock %}

{% block body %}
    <div class="container">
    <div class="form-control mt-4">
        <h2 class="mb-4 text-center">Ajouter une Ville</h2>

        {{ form_start(form) }}
        {% for type, messages in app.flashes %}
        <div class="alert alert-{{ type == 'success' ? 'success' : 'danger' }} alert-dismissible fade show" role="alert">
            {% for message in messages %}
                {{ message|raw }}
            {% endfor %}
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
        {% endfor %}        
        <div class="mb-3">
            {{ form_label(form.nomVille, 'Nom de la Ville') }}
            {{ form_widget(form.nomVille, {'attr': {'class': 'form-control'}}) }}
            {{ form_errors(form.nomVille) }}
        </div>

        <div class="mb-3">
            {{ form_label(form.departementVille, 'Département de la Ville') }}
            {{ form_widget(form.departementVille, {'attr': {'class': 'form-control'}}) }}
            {{ form_errors(form.departementVille) }}
        </div>

        <button type="submit" class="btn btn-primary">Enregistrer</button>
        
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
